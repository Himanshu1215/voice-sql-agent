services:
  api:
    build: .
    container_name: voice-sql-agent
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - WHISPER_MODEL=tiny
    restart: unless-stopped